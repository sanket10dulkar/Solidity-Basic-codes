// SPDX-License-Identifier: MIT
pragma solidity ^0.8.17;

contract Counter {
    uint public count;
    
    
    // += will do count = count + 1
    function inc() external {
        count += 1;
    }
    
    // -= will do count = count - 1
    function dec() external {
        count -= 1;
    }
}
